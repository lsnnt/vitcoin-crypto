// src/index.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // This should point to App.js in the same directory
import './styles.css'; // This should point to styles.css in the src folder

ReactDOM.render(<App />, document.getElementById('root'));
